{
  "name": "smart-trip-liveactivity-cron",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker cron that drives the SMART-trip Live Activity push endpoint on a schedule.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.103.0"
  }
}
